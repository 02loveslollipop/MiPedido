name: CI — Backend Tests

on:
  pull_request:
    paths:
      - 'backend/**'
      - '.github/**'
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
      - '.github/**'

jobs:
  lint-workflows:
    name: Lint GitHub Actions workflows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run actionlint
        run: |
          docker run --rm -v "$PWD:/repo" -w /repo rhysd/actionlint:latest -color .github/workflows/*.yml .github/workflow-templates/*.yml

  backend-tests:
    runs-on: ubuntu-latest
    services:
      mongo:
        image: mongo:6.0
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand({ping:1})'" --health-interval 10s --health-timeout 5s --health-retries 5
    env:
      PYTHONPATH: backend
      MONGODB_URL: mongodb://localhost:27017
      DATABASE_NAME: test_mipedido
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
      - name: Wait for MongoDB
        run: |
          python - <<PY
          import time, pymongo
          for _ in range(30):
              try:
                  pymongo.MongoClient('mongodb://localhost:27017', serverSelectionTimeoutMS=1000).admin.command('ping')
                  print('Mongo is up')
                  break
              except Exception as e:
                  print('Waiting for mongo...', e)
                  time.sleep(1)
          else:
              raise SystemExit('Mongo did not start')
          PY
      - name: Run backend tests
        working-directory: ./
        run: |
          pytest -q
