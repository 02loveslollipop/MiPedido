[pytest]
# The repositories bind Motor collections at import time (single event loop per
# process, as in production). Run all async tests and fixtures in one
# session-scoped loop so those clients stay bound to a live loop.
asyncio_default_test_loop_scope = session
asyncio_default_fixture_loop_scope = session
